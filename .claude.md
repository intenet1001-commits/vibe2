# 프로젝트 개요

바이브 서비스 - 내가 필요한건 내가 만들어 쓰는 시대를 위한 학습 가이드 사이트

## 기술 스택

- **프레임워크**: Next.js 15 (App Router, Turbopack)
- **언어**: TypeScript
- **스타일링**: Tailwind CSS
- **UI 컴포넌트**: shadcn/ui
- **백엔드**: Supabase (예정)

## 프로젝트 구조

```
app/
├── page.tsx              # 메인 페이지
├── layout.tsx            # 루트 레이아웃 (SeniorModeProvider 포함)
├── globals.css           # 글로벌 스타일 + 크게보기 모드
├── setup-guide/          # 개발 환경 설치 가이드
├── git-guide/            # Git 완벽 가이드
├── iterm-guide/          # iTerm2 단축키 가이드
└── shadcn-demo/          # shadcn/ui 비교 데모

components/
├── ui/                   # shadcn/ui 컴포넌트
├── quick-start.tsx       # 빠른 시작 섹션
├── nav.tsx               # 네비게이션 (크게보기 토글 버튼 포함)
└── senior-mode-provider.tsx  # 크게보기(고령자) 모드 Context
```

## 주요 기능

### 크게보기(고령자) 모드
- Nav의 "👓 크게보기" 버튼으로 토글
- localStorage에 설정 저장 (새로고침 유지)
- 반응형 폰트 사이즈 적용

### 반응형 브레이크포인트
| 화면 | 크기 | Tailwind |
|------|------|----------|
| 모바일 | 기본 | - |
| 태블릿 | 768px+ | md: |
| 데스크톱 | 1024px+ | lg: |
| 대형 | 1280px+ | xl: |

## 통일된 타이포그래피

- **h1**: `text-3xl md:text-4xl`
- **subtitle**: `text-lg md:text-xl`
- **h2**: `text-2xl`
- **본문**: `text-sm` ~ `text-base`

## 빠른 시작 순서

1. `npx create-next-app@latest . --ts --eslint --tailwind --src-dir --app --turbopack --no-import-alias`
2. `npx shadcn@latest init`
3. `/init` (Claude Code 초기화)
4. `npm run dev`

## 개발 규칙

- 한국어로 응답
- shadcn/ui 컴포넌트 우선 사용
- Tailwind CSS 유틸리티 클래스 사용
- App Router 패턴 따르기
- 폰트 사이즈는 통일된 규칙 준수
